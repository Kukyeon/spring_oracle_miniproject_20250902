<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.kkuk.miniproject.dao.BoardDao">

<resultMap type="com.kkuk.miniproject.dto.BoardDto" id="boardWithMember">
	<id property="bnum" column="bnum"/>
	<result property="rnum" column="rnum"/>
	<result property="btitle" column="btitle"/>
	<result property="bcontent" column="bcontent"/>
	<result property="bwriter" column="bwriter"/>
	<result property="bhit" column="bhit"/>
	<result property="bdate" column="bdate"/>	
	
	<association property="memberDto" javaType="com.kkuk.miniproject.dto.MemberDto">
	<id property="memberid" column="memberid"/>
	<result property="membernum" column="membernum"/>
	<result property="memberpw" column="memberpw"/>
	<result property="membername" column="membername"/>
	<result property="memberdate" column="memberdate"/>
	</association>
</resultMap>

<!-- 게시판 목록 -->
<select id="BoardListDao" resultMap="boardWithMember">
	<![CDATA[
			select * 
	    	from (select ROWNUM as rnum, r.*
	    	from
	    	(SELECT b.bnum, b.btitle, b.bcontent, b.bwriter, m.membername, b.bhit, b.bdate
			FROM boardtbl b INNER JOIN membertbl m ON b.bwriter = m.memberid
			ORDER BY b.bnum DESC) r where ROWNUM <= #{param2})
	    	where rnum >= #{param1}
	   ]]> 	
</select>

<select id="BoardCountDao" resultType="int">
	SELECT COUNT(*) 
	FROM boardtbl
</select>

<update id="boardprocessDao">
	UPDATE boardtbl 
	SET btitle =#{param2}, bcontent = #{param3} 
	WHERE bnum = #{param1}
</update>

<select id="boardviewDao" resultType="com.kkuk.miniproject.dto.BoardDto">
	SELECT * 
	FROM boardtbl 
	WHERE bnum = #{param1}
</select>

<select id="boardEditDao" resultType="com.kkuk.miniproject.dto.BoardDto">
	SELECT * 
	FROM boardtbl 
	WHERE bnum = #{param1}
</select>

<update id="updateviewDao">
	UPDATE boardtbl 
	SET bhit=bhit+1 
	WHERE bnum = #{parma1}
</update>

<insert id="WriteDao">
	INSERT INTO boardtbl(bnum, btitle, bcontent, bwriter, bhit) 
	VALUES (board_seq.nextval, #{param1}, #{param2}, #{param3}, 0)
</insert>

<delete id="boardDeleteDao">
	DELETE FROM boardtbl 
	WHERE bnum = #{param1}
</delete>
</mapper>
    